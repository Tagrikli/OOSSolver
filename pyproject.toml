[project]
name = "ooskiller"
version = "0.1.0"
description = "RL planner for a Parkolay automated storage/retrieval system"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.6",
    "pygame>=2.6.1",
    "torch>=2.5.0",
    "tensorboard>=2.18.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
