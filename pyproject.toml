[project]
name = "ooskiller"
version = "0.1.0"
description = "Deterministic plan solver for a Parkolay automated storage/retrieval system"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.6",
    "dearpygui>=2.3.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
